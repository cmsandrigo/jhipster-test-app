version: '2'
services:
    projector-mysql:
        container_name: projector-mysql
        image: mysql:5.7.13
        # volumes:
        #     - ~/volumes/jhipster/Projector/mysql/:/var/lib/mysql/
        environment:
            - MYSQL_USER=root
            - MYSQL_ALLOW_EMPTY_PASSWORD=yes
            - MYSQL_DATABASE=projector
        ports:
            - 3306:3306
        command: mysqld --lower_case_table_names=1 --skip-ssl --character_set_server=utf8
